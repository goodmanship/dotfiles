# Allow manual title setting (disable shell integration's auto-title)
shell-integration-features = no-title

# All new tabs/windows run the EPL team script (which sets title then execs zsh)
command = ~/.config/ghostty/random-epl-tab.sh

# Open new tab with fn-F6
keybind = cmd+shift+t=new_tab
